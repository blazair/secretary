# --- secrets. never commit these ---
# Kept specific: a blanket *token* pattern also swallows frontend/css/tokens.css.
token.txt
token*.txt
*.token
*_token
secret_key
*.pem
*.key
.env
instance/

# Task, note and settings data from the old single-user desktop build.
data/

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/
