# Secretary — the web build.
#
#     pip install -r requirements.txt
#
# Python >= 3.10 is required (the code uses "str | None" type hints).

flask>=3.1
waitress>=3.0          # the server used in place of Flask's development one

# Development only.
pytest>=8.0

# The planning modules themselves still depend on nothing outside the standard
# library: models, quick_add, calibration, scheduler and capacity import only
# json, re, uuid, datetime, dataclasses, collections and statistics. Flask sits
# above them and is not imported by any of them.
